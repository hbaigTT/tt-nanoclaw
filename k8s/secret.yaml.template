# Template — do NOT commit with real values.
# In production, secrets are managed by Ansible in github-ci-infra.
apiVersion: v1
kind: Secret
metadata:
  name: tt-nanoclaw
  namespace: monitoring
stringData:
  ANTHROPIC_API_KEY: "sk-ant-..."
  # Dev: points to #tt-nanoclaw (private testing channel)
  # Prod: points to #github-ci-infra-alerts
  SLACK_WEBHOOK_URL: "https://hooks.slack.com/services/..."
