apiVersion: v1
kind: Service
metadata:
  name: tt-nanoclaw
  namespace: monitoring
spec:
  selector:
    app: tt-nanoclaw
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
